specVersion: "v1"
serviceId: "viral"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "viral-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
